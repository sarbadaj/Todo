<%= form_for([@user, @user.projects.build]) do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

   <p>
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </p>
  
  <p>
    <%= f.submit :Add %>
  </p>
<% end %>